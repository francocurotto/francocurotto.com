pid /tmp/nginx.pid;

events {}

http {

    include /etc/nginx/mime.types;

    access_log /dev/stdout;
    error_log /dev/stderr;

    server {
        listen 8080;
        root /home/franco/Workspace/francocurotto.com/content;
        index index.shtml index.html;
        ssi on;
    }
}
